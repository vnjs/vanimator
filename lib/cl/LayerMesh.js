"use strict";

const { Vector2 } = require('three');

// A LayerMesh represents the vertex and face geometry of a layer. Allows for
// the construction and editing of a 2D polygon region.

function LayerMesh() {

    const vertices = [];
    const vertex_uvs = [];
    const faces_indexes = [];





}

module.exports = LayerMesh;
